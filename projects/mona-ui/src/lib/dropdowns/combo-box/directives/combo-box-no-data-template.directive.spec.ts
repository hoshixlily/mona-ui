import { ComboBoxNoDataTemplateDirective } from './combo-box-no-data-template.directive';

describe('ComboBoxNoDataTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ComboBoxNoDataTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
