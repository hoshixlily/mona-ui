import { ComboBoxFooterTemplateDirective } from './combo-box-footer-template.directive';

describe('ComboBoxFooterTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ComboBoxFooterTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
