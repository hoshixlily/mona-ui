import { ComboBoxHeaderTemplateDirective } from './combo-box-header-template.directive';

describe('ComboBoxHeaderTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ComboBoxHeaderTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
