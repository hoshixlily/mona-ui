import { ComboBoxGroupHeaderTemplateDirective } from "./combo-box-group-header-template.directive";

describe("ComboBoxGroupTemplateDirective", () => {
    it("should create an instance", () => {
        const directive = new ComboBoxGroupHeaderTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
