import { DropDownTreeHeaderTemplateDirective } from './drop-down-tree-header-template.directive';

describe('DropDownTreeHeaderTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDownTreeHeaderTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
