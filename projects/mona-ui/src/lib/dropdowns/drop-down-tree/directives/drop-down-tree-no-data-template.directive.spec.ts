import { DropDownTreeNoDataTemplateDirective } from './drop-down-tree-no-data-template.directive';

describe('DropDownTreeNoDataTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDownTreeNoDataTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
