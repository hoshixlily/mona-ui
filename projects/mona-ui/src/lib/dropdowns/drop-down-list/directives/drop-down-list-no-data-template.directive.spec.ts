import { DropDownListNoDataTemplateDirective } from './drop-down-list-no-data-template.directive';

describe('DropDownListNoDataTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDownListNoDataTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
