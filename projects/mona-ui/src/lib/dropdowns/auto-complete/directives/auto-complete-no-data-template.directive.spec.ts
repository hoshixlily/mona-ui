import { AutoCompleteNoDataTemplateDirective } from './auto-complete-no-data-template.directive';

describe('AutoCompleteNoDataTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCompleteNoDataTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
