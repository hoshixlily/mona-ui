import { AutoCompleteGroupHeaderTemplateDirective } from './auto-complete-group-header-template.directive';

describe('AutoCompleteGroupHeaderTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCompleteGroupHeaderTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
