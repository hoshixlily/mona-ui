import { AutoCompleteFooterTemplateDirective } from './auto-complete-footer-template.directive';

describe('AutoCompleteFooterTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCompleteFooterTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
