import { AutoCompleteItemTemplateDirective } from './auto-complete-item-template.directive';

describe('AutoCompleteItemTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCompleteItemTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
