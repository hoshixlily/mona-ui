import { AutoCompleteHeaderTemplateDirective } from './auto-complete-header-template.directive';

describe('AutoCompleteHeaderTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCompleteHeaderTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
