import { MultiSelectFooterTemplateDirective } from './multi-select-footer-template.directive';

describe('MultiSelectFooterTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiSelectFooterTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
