import { MultiSelectHeaderTemplateDirective } from './multi-select-header-template.directive';

describe('MultiSelectHeaderTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiSelectHeaderTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
