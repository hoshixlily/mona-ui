import { MultiSelectNoDataTemplateDirective } from './multi-select-no-data-template.directive';

describe('MultiSelectNoDataTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiSelectNoDataTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
