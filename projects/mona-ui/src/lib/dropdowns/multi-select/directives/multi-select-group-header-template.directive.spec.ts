import { MultiSelectGroupHeaderTemplateDirective } from "./multi-select-group-header-template.directive";

describe("MultiSelectGroupHeaderTemplateDirective", () => {
    it("should create an instance", () => {
        const directive = new MultiSelectGroupHeaderTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
