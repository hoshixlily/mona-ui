import { MultiSelectTagTemplateDirective } from "./multi-select-tag-template.directive";

describe("MultiSelectTagTemplateDirective", () => {
    it("should create an instance", () => {
        const directive = new MultiSelectTagTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
