import { NumericTextBoxPrefixTemplateDirective } from './numeric-text-box-prefix-template.directive';

describe('NumericTextBoxPrefixTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericTextBoxPrefixTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
