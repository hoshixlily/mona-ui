import { SwitchOffLabelTemplateDirective } from "./switch-off-label-template.directive";

describe("SwitchOffLabelTemplateDirective", () => {
    it("should create an instance", () => {
        const directive = new SwitchOffLabelTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
