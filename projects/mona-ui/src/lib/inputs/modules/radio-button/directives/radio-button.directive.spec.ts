import { RadioButtonDirective } from "./radio-button.directive";

describe("RadioButtonDirective", () => {
    it("should create an instance", () => {
        const directive = new RadioButtonDirective();
        expect(directive).toBeTruthy();
    });
});
