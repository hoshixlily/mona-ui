import { SliderTickValueTemplateDirective } from "./slider-tick-value-template.directive";

describe("SliderTickValueTemplateDirective", () => {
    it("should create an instance", () => {
        const directive = new SliderTickValueTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
