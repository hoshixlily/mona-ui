import { TextBoxSuffixTemplateDirective } from "./text-box-suffix-template.directive";

describe("TextBoxSuffixTemplateDirective", () => {
    it("should create an instance", () => {
        const directive = new TextBoxSuffixTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
