import { PopoverTitleTemplateDirective } from './popover-title-template.directive';

describe('PopoverTitleTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverTitleTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
