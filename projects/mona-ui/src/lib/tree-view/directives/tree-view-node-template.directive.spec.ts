import { TreeViewNodeTemplateDirective } from './tree-view-node-template.directive';

describe('TreeViewNodeTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeViewNodeTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
