import { ListBoxNoDataTemplateDirective } from './list-box-no-data-template.directive';

describe('ListBoxNoDataTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ListBoxNoDataTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
