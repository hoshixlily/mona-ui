import { SplitButtonTextTemplateDirective } from "./split-button-text-template.directive";

describe("SplitButtonTextTemplateDirective", () => {
    it("should create an instance", () => {
        const directive = new SplitButtonTextTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
