import { DateIncludePipe } from './date-include.pipe';

describe('DateIncludePipe', () => {
  it('create an instance', () => {
    const pipe = new DateIncludePipe();
    expect(pipe).toBeTruthy();
  });
});
