import { PopupAnchorDirective } from './popup-anchor.directive';

describe('PopupAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
