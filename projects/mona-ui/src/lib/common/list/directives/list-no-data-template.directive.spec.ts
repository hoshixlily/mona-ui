import { ListNoDataTemplateDirective } from './list-no-data-template.directive';

describe('ListNoDataTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ListNoDataTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
