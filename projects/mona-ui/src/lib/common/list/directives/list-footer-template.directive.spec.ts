import { ListFooterTemplateDirective } from './list-footer-template.directive';

describe('ListFooterTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ListFooterTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
