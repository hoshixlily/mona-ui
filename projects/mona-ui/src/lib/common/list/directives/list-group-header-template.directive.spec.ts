import { ListGroupHeaderTemplateDirective } from './list-group-header-template.directive';

describe('ListGroupHeaderTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ListGroupHeaderTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
