import { ListFilterableDirective } from './list-filterable.directive';

describe('ListFilterableDirective', () => {
  it('should create an instance', () => {
    const directive = new ListFilterableDirective();
    expect(directive).toBeTruthy();
  });
});
