import { ListNavigableDirective } from './list-navigable.directive';

describe('ListNavigableDirective', () => {
  it('should create an instance', () => {
    const directive = new ListNavigableDirective();
    expect(directive).toBeTruthy();
  });
});
