import { ListSelectableDirective } from './list-selectable.directive';

describe('ListSelectableDirective', () => {
  it('should create an instance', () => {
    const directive = new ListSelectableDirective();
    expect(directive).toBeTruthy();
  });
});
