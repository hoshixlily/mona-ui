import { ListGroupableDirective } from './list-groupable.directive';

describe('ListGroupableDirective', () => {
  it('should create an instance', () => {
    const directive = new ListGroupableDirective();
    expect(directive).toBeTruthy();
  });
});
