import { TreeFilterableDirective } from './tree-filterable.directive';

describe('TreeFilterableDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeFilterableDirective();
    expect(directive).toBeTruthy();
  });
});
