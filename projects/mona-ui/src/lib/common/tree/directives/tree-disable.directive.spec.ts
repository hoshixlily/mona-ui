import { TreeDisableDirective } from './tree-disable.directive';

describe('TreeDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeDisableDirective();
    expect(directive).toBeTruthy();
  });
});
