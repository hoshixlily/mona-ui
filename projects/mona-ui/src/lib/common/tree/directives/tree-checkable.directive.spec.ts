import { TreeCheckableDirective } from './tree-checkable.directive';

describe('TreeCheckableDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeCheckableDirective();
    expect(directive).toBeTruthy();
  });
});
