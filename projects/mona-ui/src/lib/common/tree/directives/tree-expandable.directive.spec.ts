import { TreeExpandableDirective } from './tree-expandable.directive';

describe('TreeExpandableDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeExpandableDirective();
    expect(directive).toBeTruthy();
  });
});
