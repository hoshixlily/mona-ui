import { FieldsetLegendTemplateDirective } from "./fieldset-legend-template.directive";

describe("FieldsetLegendTemplateDirective", () => {
    it("should create an instance", () => {
        const directive = new FieldsetLegendTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
