import { ExpansionPanelTitleTemplateDirective } from './expansion-panel-title-template.directive';

describe('ExpansionPanelTitleTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpansionPanelTitleTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
