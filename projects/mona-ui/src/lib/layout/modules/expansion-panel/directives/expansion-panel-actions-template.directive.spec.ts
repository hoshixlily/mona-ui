import { ExpansionPanelActionsTemplateDirective } from './expansion-panel-actions-template.directive';

describe('ExpansionPanelActionsTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpansionPanelActionsTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
